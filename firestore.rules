
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    
    // UYARI: Bu kurallar test amaçlıdır ve tüm verilere tam erişim izni verir.
    // Canlı ortamda kullanılmamalıdır.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
