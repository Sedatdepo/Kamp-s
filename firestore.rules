rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents/{document=**} {
    // GELİŞTİRME KURALI: Giriş yapmış olan tüm kullanıcılar tüm verilere erişebilir.
    // Bu kural, üretim ortamı için GÜVENLİ DEĞİLDİR.
    allow read, write: if request.auth != null;
  }
}
