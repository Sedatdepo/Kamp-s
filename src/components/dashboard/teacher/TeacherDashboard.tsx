"use client";

import { SınıfModulu } from "./SınıfModulu";

export function TeacherDashboard() {
  // The user's code is a self-contained application.
  // We will render it directly here, replacing the old dashboard.
  return <SınıfModulu />;
}
